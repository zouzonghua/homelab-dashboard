services:
  homelab-dashboard:
    image: zouzonghua/homelab-dashboard:latest
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../data:/data
    restart: unless-stopped
